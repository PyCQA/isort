init:
  - "SET PATH=C:\\Python27\\Scripts;%PATH%"
  - "SET PYTEST_ADDOPTS=-vv"
  - "ECHO PATH=%PATH%"
  - python --version

build: off

install:
  - pip install tox

test_script:
  - tox -e isort-check,py34,py35,py36,py37
